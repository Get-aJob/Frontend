## 2026-04-01 1

### 질문
@prompts/공고댓글_기능기획서.md 의 3. 댓글 작성 연결 기능을 진행하려고 해. 직접 코드를 수정하지말고 코드를 보여줘. 내가 수정할게. 그리고 url 경로는 localhost:3000/jobs/:jobId/comments야.

### 답변
- `VITE_API_URL`이 `http://localhost:3000`이면 axios에서는 경로만 `/jobs/:jobId/comments`로 호출하면 됨.
- 타입 파일, API 모듈, `PostingFeedDetail`에서 호출 예시 코드를 제공함.

---

## 2026-04-01 2

### 질문
@src/view/PostingFeedDetail.tsx 이 잘 수정되었나 확인하고, 틀렸으면 전체 코드를 다시한번 보여줘.

### 답변
- `contents`/`setComments`/`comments` 상태명 불일치로 `comments` 미정의 오류가 있었음을 확인함.
- `setIsSubmitting(false)`를 `finally`에 추가하고, `submitError` 표시·입력/버튼 `disabled` 처리, `authorName` 등 `FeedComment` 타입 보정을 반영한 전체 코드로 수정함.

---

## 2026-04-01 3

### 질문
@src/view/PostingFeedDetail.tsx 부분은 직접 수정해줘.

### 답변
- 잘못 삽입된 전역 코드를 제거하고 `getJobComments`로 목록 로드·로딩·조회 에러 UI를 추가함.
- `authorUserId`와 `userInfo.id`로 「나」 구분, 작성 응답은 `{ comment }` 및 평면 형태 모두 처리함.

---

## 2026-04-01 4

### 질문
근데 현재 @src/view/PostingFeedDetail.tsx 를 새로고침하면 회사정보가 사라지는 현상이 있어. 해당 원인을 파악하고 수정해줘.

### 답변
- 원인: `usePostingStore`의 `postings`가 persist 없이 메모리만 사용되어 새로고침 시 비어 `job` 조회가 실패함.
- 대응: `postingFeedJobPreview` 유틸로 목록 클릭 시 `sessionStorage`에 공고 스냅샷 저장, 상세에서는 스토어 → 세션 순으로 헤더 복구. 세션도 없으면 `fetchPostings(1)`로 1페이지 재조회 시도함.
